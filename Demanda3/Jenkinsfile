pipeline {
    agent any

    stages {
        stage('SCM Checkout') {
            steps {
                git branch: 'main', credentialsId: 'jk-gh-tk', url: 'https://github.com/Franck-Patrick/Desafio_DellTech.git'
            }
        }

        stage('Build Docker image') {
            steps {
                sh 'docker build -f /mnt/c/Users/Franck\\ Patrick/Downloads/Desafio_DellTech/Demanda3/Dockerfile -t pkdu14/webapp:$BUILD_NUMBER .'
            }
        }
    }
}
